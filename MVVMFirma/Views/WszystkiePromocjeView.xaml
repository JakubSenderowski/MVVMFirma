<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkiePromocjeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID Promocji" Binding="{Binding PromocjaID}"/>
                <DataGridTextColumn Header="Opis" Binding="{Binding Opis}"/>
                <DataGridTextColumn Header="TypBiletu" Binding="{Binding TypBiletu}"/>
                <DataGridTextColumn Header="Rabat" Binding="{Binding Rabat}"/>
                <DataGridTextColumn Header="DataRozpoczecia" Binding="{Binding DataRozpoczecia}"/>
                <DataGridTextColumn Header="DataZakonczenia" Binding="{Binding DataZakonczenia}"/>
                <DataGridTextColumn Header="Tytul" Binding="{Binding Tytul}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
