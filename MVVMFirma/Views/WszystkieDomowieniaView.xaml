<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkieDomowieniaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID Domowienia" Binding="{Binding IdDomowienia}"/>
                <DataGridTextColumn Header="Typ Biletu" Binding="{Binding TypBiletu}"/>
                <DataGridTextColumn Header="Towar" Binding="{Binding NazwaTowaru}"/>
                <DataGridTextColumn Header="Ilosc" Binding="{Binding Ilosc}"/>
                <DataGridTextColumn Header="Cena" Binding="{Binding Cena}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
