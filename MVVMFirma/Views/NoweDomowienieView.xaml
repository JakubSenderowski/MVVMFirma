<local:JedenViewBase x:Class="MVVMFirma.Views.NoweDomowienieView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Bilet -->
        <Label Grid.Column="0" Grid.Row="0" Margin="5" VerticalAlignment="Center" Content="Bilet" />
        <TextBox Grid.Column="1" Grid.Row="0" Margin="5" Width="200" HorizontalAlignment="Left" Text="{Binding Path=BiletID, UpdateSourceTrigger=PropertyChanged}" />

        <Label Grid.Column="0" Grid.Row="1" Margin="5" VerticalAlignment="Center" Content="Towar" />
        <TextBox Grid.Column="1" Grid.Row="1" Margin="5" Width="200" HorizontalAlignment="Left" Text="{Binding Path=TowarID, UpdateSourceTrigger=PropertyChanged}" />

        <Label Grid.Column="0" Grid.Row="2" Margin="5" VerticalAlignment="Center" Content="Ilosc" />
        <TextBox Grid.Column="1" Grid.Row="2" Margin="5" Width="200" HorizontalAlignment="Left" Text="{Binding Path=Ilosc, UpdateSourceTrigger=PropertyChanged}" />

        <Label Grid.Column="0" Grid.Row="3" Margin="5" VerticalAlignment="Center" Content="Cena" />
        <TextBox Grid.Column="1" Grid.Row="3" Margin="5" Width="200" HorizontalAlignment="Left" Text="{Binding Path=Cena, UpdateSourceTrigger=PropertyChanged}" />


    </Grid>
</local:JedenViewBase>
